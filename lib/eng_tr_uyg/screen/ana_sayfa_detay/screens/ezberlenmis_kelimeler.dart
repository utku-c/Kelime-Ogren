import 'package:flutter/material.dart';

class EzberlenmisKelime extends StatelessWidget {
  const EzberlenmisKelime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
